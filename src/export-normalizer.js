import * as THREE from 'https://esm.sh/three@0.161.0';
import { STLExporter } from 'https://esm.sh/three@0.161.0/examples/jsm/exporters/STLExporter.js';
const originalParse=STLExporter.prototype.parse;
STLExporter.prototype.parse=function(scene,...args){scene.updateMatrixWorld(true);const box=new THREE.Box3().setFromObject(scene);if(!box.isEmpty()){scene.position.x-=box.min.x;scene.position.y-=box.min.y;scene.position.z-=box.min.z;scene.updateMatrixWorld(true)}return originalParse.call(this,scene,...args)};
const saveProject=()=>{const $=id=>document.getElementById(id),data={app:'Gemelos 3D',project:'Proyecto 02',printer:$('printer')?.value,custom:{x:$('cx')?.value,y:$('cy')?.value,z:$('cz')?.value},margin:+($('margin')?.value||0),purgeMode:$('purgeMode')?.value,purge:{x:+($('purgeX')?.value||0),y:+($('purgeY')?.value||0)},text:$('text')?.value,fontStyle:$('fontStyle')?.value,height:+($('height')?.value||0),widthScale:+($('widthScale')?.value||0),depth:+($('depth')?.value||0),spacing:+($('spacing')?.value||0),curveSegments:+($('curveSegments')?.value||0),bevel:!!$('bevel')?.checked,bevelSize:+($('bevelSize')?.value||0),bevelSegments:+($('bevelSegments')?.value||0)};const url=URL.createObjectURL(new Blob([JSON.stringify(data,null,2)],{type:'application/json'})),a=document.createElement('a');a.href=url;a.download='gemelos3d-proyecto02.json';a.click();setTimeout(()=>URL.revokeObjectURL(url),1000)};
const wire=()=>{document.getElementById('project')?.addEventListener('click',saveProject);const b=document.getElementById('bevel'),f=document.getElementById('bevelFields');if(b&&f){f.hidden=!b.checked;b.addEventListener('change',()=>f.hidden=!b.checked)}};
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',wire,{once:true});else wire();
